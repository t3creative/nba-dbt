# models/intermediate/player_scoring/schema.yml
version: 2

models:
  - name: int_player_scoring_features
    description: Final aggregated player scoring features table
    columns:
      - name: player_game_key
        description: Unique identifier for player-game combination
        tests:
          - unique
          - not_null
